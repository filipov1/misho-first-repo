# Използваме официалния Node.js образ като базов
FROM node:14

# Настройваме директория за приложението
WORKDIR /usr/src/app

# Копираме package.json и инсталираме зависимостите
COPY package*.json ./
RUN npm install

# Копираме всички файлове в контейнера
COPY . .

# Променяме порта, на който ще работи приложението
EXPOSE 8080

# Стартираме приложението
CMD [ "node", "index.js" ]
